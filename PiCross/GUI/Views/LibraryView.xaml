<UserControl x:Class="GUI.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:GUI.Views"
             xmlns:controls="clr-namespace:GUI.Controls"
             xmlns:converters="clr-namespace:GUI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        
    </UserControl.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Bottom">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ToggleButton Style="{DynamicResource filterButtonStyle}" IsChecked="{Binding ShowSolved.Value}" ToolTip="Toggle show solved" />
                <Button Style="{DynamicResource backButtonStyle}" Command="{Binding Back}" ToolTip="Back" />
            </StackPanel>
        </Border>

        <views:GeneralizedLibraryView Groups="{Binding Groups.Value}">
            <views:GeneralizedLibraryView.GroupHeaderTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Foreground="White" FontSize="64" HorizontalAlignment="Center" Margin="0,20,0,20">
                            <TextBlock.Text>
                                <Binding Path="Size">
                                    <Binding.Converter>
                                        <converters:InterpolatedStringConverter String="[[Width]] x [[Height]]" />
                                    </Binding.Converter>
                                </Binding>
                            </TextBlock.Text>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </views:GeneralizedLibraryView.GroupHeaderTemplate>
            <views:GeneralizedLibraryView.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding Select}" Style="{DynamicResource libraryButtonStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="auto" />
                            </Grid.RowDefinitions>
                            <ContentControl>
                                <ContentControl.Content>
                                    <Binding Path="Solved">
                                        <Binding.Converter>
                                            <converters:TemplateLoaderConverter>
                                                <converters:BoolConverter>
                                                    <converters:BoolConverter.True>
                                                        <DataTemplate>
                                                            <DockPanel>
                                                                <TextBlock DockPanel.Dock="Top" Text="{Binding BestTime}" HorizontalAlignment="Center" />
                                                                <Viewbox Grid.Row="0" Width="100" Height="100">
                                                                    <controls:ThumbnailControl Grid="{Binding Grid}" />
                                                                </Viewbox>
                                                            </DockPanel>
                                                        </DataTemplate>
                                                    </converters:BoolConverter.True>
                                                    <converters:BoolConverter.False>
                                                        <DataTemplate>
                                                            <Viewbox Width="100" Height="100">
                                                                <TextBlock Text="?" />
                                                            </Viewbox>
                                                        </DataTemplate>
                                                    </converters:BoolConverter.False>
                                                </converters:BoolConverter>
                                            </converters:TemplateLoaderConverter>
                                        </Binding.Converter>
                                    </Binding>
                                </ContentControl.Content>
                            </ContentControl>
                            <TextBlock Grid.Row="1" FontSize="32" HorizontalAlignment="Center">
                                <TextBlock.Text>
                                    <Binding Path="Puzzle.Size">
                                        <Binding.Converter>
                                            <converters:InterpolatedStringConverter String="[[Width]] x [[Height]]" />
                                        </Binding.Converter>
                                    </Binding>
                                </TextBlock.Text>
                            </TextBlock>
                        </Grid>
                    </Button>
                </DataTemplate>
            </views:GeneralizedLibraryView.ItemTemplate>
        </views:GeneralizedLibraryView>
    </DockPanel>
</UserControl>
