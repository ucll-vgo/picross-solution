<UserControl x:Class="GUI.Views.IntroView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="PiCross" FontSize="128" HorizontalAlignment="Center" VerticalAlignment="Bottom" Foreground="White" />
        <StackPanel Orientation="Vertical" Grid.Row="1">
            <Button Content="Play" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Top" Padding="10" Margin="10" Width="200" Command="{Binding Play}" />
            <Button Content="Edit" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Top" Padding="10" Margin="10" Width="200" Command="{Binding Edit}" />
        </StackPanel>
    </Grid>
</UserControl>
