<UserControl x:Class="GUI.Views.IntroView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Viewbox Margin="20">
            <TextBlock Style="{DynamicResource introTitleStyle}" Grid.Row="0" Text="PiCross" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Viewbox>
        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="Play" Command="{Binding Play}" Style="{DynamicResource introButtonStyle}" />
            <Button Content="Edit" Command="{Binding Edit}" Style="{DynamicResource introButtonStyle}" />
            <Button Content="Quit" Command="{Binding Quit}" Style="{DynamicResource introButtonStyle}" />
        </StackPanel>
    </Grid>
</UserControl>
