<UserControl x:Class="GUI.Controls.EditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             xmlns:aux="clr-namespace:PiCross.Controls;assembly=Controls"
             xmlns:controls="clr-namespace:GUI.Controls"
             xmlns:converters="clr-namespace:GUI.Converters"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <controls:PuzzleDataConverter x:Key="puzzleDataConverter" />
    </UserControl.Resources>
    <aux:PiCrossControl x:Name="editorControl" PuzzleData="{Binding Path=ViewModel, RelativeSource={RelativeSource AncestorType={x:Type controls:EditorControl}}, Converter={StaticResource puzzleDataConverter}}" Margin="5" HorizontalAlignment="Center" VerticalAlignment="Center">
        <aux:PiCrossControl.SquareTemplate>
            <DataTemplate>
                <controls:EditorGridSquareControl Contents="{Binding IsFilled.Value}"
                                                  Width="24"
                                                  Height="24"
                                                  Ambiguity="{Binding Ambiguity.Value}"
                                                  Activate="{Binding Activate}"
                                                  LeftClick="{Binding SetFilled}"
                                                  RightClick="{Binding SetEmpty}">
                </controls:EditorGridSquareControl>
            </DataTemplate>
        </aux:PiCrossControl.SquareTemplate>

        <aux:PiCrossControl.ColumnConstraintsTemplate>
            <DataTemplate>
                <DockPanel LastChildFill="False" Margin="1" Style="{DynamicResource Editor_Constraints_Style}">
                    <ItemsControl ItemsSource="{Binding Constraints.Value}" DockPanel.Dock="Bottom">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid VerticalAlignment="Stretch">
                                    <TextBlock Text="{Binding .}"
                                               Margin="0,5"
                                               HorizontalAlignment="Center" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </DockPanel>
            </DataTemplate>
        </aux:PiCrossControl.ColumnConstraintsTemplate>

        <aux:PiCrossControl.RowConstraintsTemplate>
            <DataTemplate>
                <DockPanel LastChildFill="False" Margin="1" Style="{DynamicResource Editor_Constraints_Style}">
                    <ItemsControl ItemsSource="{Binding Constraints.Value}" DockPanel.Dock="Right">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid VerticalAlignment="Stretch">
                                    <TextBlock Text="{Binding .}"
                                               Margin="5,0"
                                               VerticalAlignment="Center" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </DockPanel>
            </DataTemplate>
        </aux:PiCrossControl.RowConstraintsTemplate>

        <aux:PiCrossControl.Thumbnail>
            <controls:ThumbnailControl Grid="{Binding ThumbnailData}" PixelSize="2" />
        </aux:PiCrossControl.Thumbnail>
    </aux:PiCrossControl>
</UserControl>
